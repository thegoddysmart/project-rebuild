## What

Add pnpm approve-builds approvals by running the GitHub Actions workflow.

## Why

Vercel blocks certain lifecycle scripts by default; committing approvals prevents build failures.

## Notes

- This PR is created by CI and contains generated approval files. Review if needed.
